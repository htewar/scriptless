import { AVAILABLE_COMMANDS } from "../utils/data";

// export const GetCommandOutput = (input: string): string => {
//     const commandList = input.split(" ");
//     const firstCommand = commandList[0];
//     return AVAILABLE_COMMANDS[firstCommand]
// }